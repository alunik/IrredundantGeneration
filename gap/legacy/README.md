# Legacy GAP entry points

These files are historical case-specific scripts and older one-shot runners
retained to explain older log names and intermediate computations.  They are
not the public recomputation interface.

Use the group-agnostic runners listed in `data/certificate_runs.json` for new
or referee-facing runs.
