# Legacy GAP entry points

These files are historical case-specific scripts retained to explain older log
names and intermediate computations.  They are not the public recomputation
interface.

Use the group-agnostic runners in `gap/42_*.g` through `gap/47_*.g`, with
settings from `data/certificate_runs.json`, for new or referee-facing runs.
